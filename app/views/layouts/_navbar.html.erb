<nav class="navbar navbar-expand navbar-dark bg-dark static-top">
  <a class="navbar-brand mr-1" href="/">Task Manager</a>
<!-- Navbar Search -->
  <div class="d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0">
    <%= form_tag(tasks_path, method: :get, class: 'input-group') do %>
      <%= text_field_tag :tag, params[:tag], class: 'form-control', placeholder: 'Search for...' %>
      <div class="input-group-append">
        <%= button_tag :submit, class: "btn btn-primary" do %>
          <i class="fas fa-search"></i>
        <% end %>
      </div>
    <% end %>
  </div>

<!-- Navbar -->
  <ul class="navbar-nav ml-auto ml-md-0">
    <li class="nav-item dropdown no-arrow">
      <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fas fa-user-circle fa-fw"></i>
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
        <a class="dropdown-item" href="/users">Profile</a>
        <div class="dropdown-divider"></div>
        <%= link_to "Logout", logout_path, method: "delete", class: "dropdown-item" %>
      </div>
    </li>
  </ul>
</nav>
